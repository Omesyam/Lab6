#include <iostream>
#include <Windows.h>
#include <math.h>
#include<stdio.h>
#define _Use_Math_Defines

using namespace std;
int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    int x, b, c, d, F;
    cout << "Введіть x:";
    cin >> x;
    cout << "Введіть b:";
    cin >> b;
    cout << "Введіть c:";
    cin >> c;
    cout << "Введіть d:";
    cin >> d;
    if (x + 5 < 0 && c == 0) {
        F = (1 / a * x) - b;
        }
    else {
        if (x + 5 > 0 && x != 0) {
            F = (x - a) / x;
        }
        else {
            if (c != 4) {
                F = (10 * x) / c - 4;
            }
            else {
                cout << "c=0";
            }
        }
    }
    cout << "F=" << F << endl;
 
    system("pause");
    return 0;

}